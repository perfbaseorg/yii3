name: PHP CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  php-tests:
    name: PHPStan and PHPUnit on PHP ${{ matrix.php-version }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        php-version: ["8.2", "8.3", "8.4"]

    steps:
      - name: Checkout yii3
        uses: actions/checkout@v4
        with:
          path: yii3

      - name: Checkout php-sdk sibling
        uses: actions/checkout@v4
        with:
          repository: perfbaseorg/php-sdk
          path: lib-php-sdk

      - name: Set up PHP ${{ matrix.php-version }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}
          tools: composer

      - name: Install Perfbase extension
        run: bash -c "$(curl -fsSL https://cdn.perfbase.com/install.sh)"

      - name: Install dependencies
        working-directory: yii3
        run: composer install --prefer-dist --no-progress

      - name: Run PHPStan
        working-directory: yii3
        run: composer run phpstan

      - name: Run PHPUnit
        working-directory: yii3
        run: composer run test
